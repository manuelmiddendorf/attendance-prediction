# TODO

## Synthetic data

- [ ] Ensure that all member collection columns are valid JSON arrays.
- [ ] Represent empty collections as `[]` instead of empty CSV cells.
- [ ] Verify that the synthetic schema matches the production schema.
